# OS / IDE
.DS_Store
.idea/
.vscode/

# Build
bin/
build/
*.out

# Kubernetes / Helm
*.tgz
kubeconfig
kubeconfig.yaml
*.kubeconfig
admin.conf

# Secrets (actual secret values only)
.env
*.secret.yaml
# Note: platform/external-secrets/secrets/ contains ExternalSecret CRDs
# (references to secrets, not actual values) - those are tracked in Git

# Terraform (if used later)
.terraform/
*.tfstate
*.tfstate.backup

# Flux
.flux.yaml

# Additional env patterns (but not .env.example)
!.env.example

# Kubeconfig
admin.conf

# Helm charts
**/charts/

# Talos images
*.raw
*.raw.xz

# Talos
talosconfig
*.talosconfig
